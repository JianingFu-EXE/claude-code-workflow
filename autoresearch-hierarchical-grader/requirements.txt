# Runtime dependencies
pyyaml>=6.0

# External tooling (must be on PATH)
# - claude CLI (Claude Code): dispatches subagents via `claude --print -p`
#   Install: https://docs.anthropic.com/en/docs/claude-code
